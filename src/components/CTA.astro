---
import Icon from './Icons.astro';
---

<section id="contact" class="py-20 bg-gradient-to-r from-blue-600 to-green-600">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center text-white">
    <h2 class="text-4xl font-bold mb-4">Get Your Free Compliance Audit</h2>
    <p class="text-xl mb-8 opacity-95">
      Find out if your generators are truly AHCA-compliant. We'll review your current maintenance program and identify any gaps - absolutely free.
    </p>

    <div class="bg-white/10 backdrop-blur-sm rounded-lg p-8 max-w-2xl mx-auto">
      <form class="space-y-4">
        <div class="grid md:grid-cols-2 gap-4">
          <input
            type="text"
            placeholder="Facility Name"
            class="w-full px-4 py-3 rounded-lg text-gray-900 placeholder-gray-500"
            required
          />
          <input
            type="text"
            placeholder="Your Name"
            class="w-full px-4 py-3 rounded-lg text-gray-900 placeholder-gray-500"
            required
          />
        </div>
        <div class="grid md:grid-cols-2 gap-4">
          <input
            type="email"
            placeholder="Email Address"
            class="w-full px-4 py-3 rounded-lg text-gray-900 placeholder-gray-500"
            required
          />
          <input
            type="tel"
            placeholder="Phone Number"
            class="w-full px-4 py-3 rounded-lg text-gray-900 placeholder-gray-500"
            required
          />
        </div>
        <select class="w-full px-4 py-3 rounded-lg text-gray-900">
          <option>Select Facility Type</option>
          <option>Assisted Living Facility</option>
          <option>Nursing Home</option>
          <option>Hospital</option>
          <option>Medical Center</option>
          <option>Other Healthcare Facility</option>
        </select>
        <textarea
          placeholder="Tell us about your current generator maintenance situation (optional)"
          rows="3"
          class="w-full px-4 py-3 rounded-lg text-gray-900 placeholder-gray-500"
        ></textarea>
        <button
          type="submit"
          class="w-full bg-green-500 hover:bg-green-600 text-white font-bold py-4 px-8 rounded-lg text-lg transition-all transform hover:scale-105"
        >
          Get My Free Compliance Audit
        </button>
      </form>
    </div>

    <div class="mt-8">
      <p class="text-lg mb-4">Or call us directly:</p>
      <a href="tel:305-XXX-XXXX" class="text-3xl font-bold hover:underline inline-flex items-center gap-2">
        <Icon name="phone" size={28} class="inline-block" />
        (305) XXX-XXXX
      </a>
      <p class="mt-2 text-sm opacity-75">Available 24/7 for emergencies</p>
    </div>
  </div>
</section>