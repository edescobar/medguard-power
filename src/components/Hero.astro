---
---

<header class="header">
  <div class="container nav">
    <div class="logo">
      <span class="logo__mark">MedGuard</span>
      <svg class="logo__starburst" viewBox="0 0 24 24" fill="currentColor">
        <path d="M12 2l1.5 4.5L18 8l-4.5 1.5L12 14l-1.5-4.5L6 8l4.5-1.5L12 2zm0 4l-.8 2.4L8.8 9.2l2.4.8L12 12.4l.8-2.4 2.4-.8-2.4-.8L12 6zm5 6l1 3 3 1-3 1-1 3-1-3-3-1 3-1 1-3zm-10 0l1 3 3 1-3 1-1 3-1-3-3-1 3-1 1-3z"/>
      </svg>
      <span class="logo__tagline">Power Services</span>
    </div>
    <nav class="nav__links">
      <a href="#services" class="nav__link">Services</a>
      <a href="#compliance" class="nav__link">Compliance</a>
      <a href="#contact" class="nav__link">Contact</a>
      <a href="tel:305-XXX-XXXX" class="btn btn--primary">Get Quote</a>
    </nav>
  </div>
</header>

<section class="hero section">
  <div class="container hero__content">
    <p class="hero__eyebrow fade-up">Healthcare-Only • Miami-Dade & Broward</p>

    <h1 class="hero__title fade-up display" style="animation-delay: 0.1s">
      Generator Compliance & Rapid Response
    </h1>

    <p class="hero__subtitle fade-up" style="animation-delay: 0.2s">
      Never fail an AHCA inspection again. We keep your generators running and your facility compliant with digital documentation and 4-hour emergency response.
    </p>

    <div class="grid grid-3 mt-4 fade-up" style="animation-delay: 0.3s">
      <div class="card">
        <div class="card__icon">🏥</div>
        <h3 class="card__title">Healthcare Only</h3>
        <p class="card__text">We exclusively serve medical facilities</p>
      </div>
      <div class="card">
        <div class="card__icon">⚡</div>
        <h3 class="card__title">4-Hour Response</h3>
        <p class="card__text">Emergency service when you need it</p>
      </div>
      <div class="card">
        <div class="card__icon">📱</div>
        <h3 class="card__title">Digital Compliance</h3>
        <p class="card__text">24/7 portal access to all records</p>
      </div>
    </div>

    <div class="hero__actions fade-up" style="animation-delay: 0.4s">
      <a href="#contact" class="btn btn--primary btn--wide">
        Get Free Compliance Audit
      </a>
      <a href="tel:305-XXX-XXXX" class="btn btn--secondary btn--wide">
        📞 (305) XXX-XXXX
      </a>
    </div>

    <div class="badge badge--orange mt-3 fade-up" style="animation-delay: 0.5s">
      ⚠️ Hurricane Season Ready - Is Your Generator?
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    background: linear-gradient(135deg,
      color-mix(in srgb, var(--clr-bg-warm) 95%, var(--clr-coral) 5%),
      color-mix(in srgb, var(--clr-bg-warm) 98%, var(--clr-brand-teal) 2%)
    );
  }

  .hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image:
      repeating-linear-gradient(
        45deg,
        transparent,
        transparent 35px,
        rgba(242, 92, 42, 0.03) 35px,
        rgba(242, 92, 42, 0.03) 70px
      );
    pointer-events: none;
  }

  @media (max-width: 768px) {
    .nav__links {
      display: none;
    }

    .hero__title {
      font-size: clamp(2rem, 6vw, 3rem);
    }
  }
</style>